"""
Authentication Models Module

This module defines database models for user authentication and profile
management in the Bridge Online School platform. Currently utilizes
Django's built-in User model for authentication functionality.

Future model extensions for user profiles and authentication-related
data structures would be defined here.
"""

from django.db import models

# Currently using Django's built-in User model for authentication
# Custom authentication models and user profile extensions can be added here as needed
